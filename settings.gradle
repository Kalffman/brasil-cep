pluginManagement {
    repositories {
        maven { url "https://repo.spring.io/milestone" }
        gradlePluginPortal()
    }
}

rootProject.name = "brasil-cep"

include "domain"
include "adapter:output:persistence:postgres"
include "adapter:input:rest"
